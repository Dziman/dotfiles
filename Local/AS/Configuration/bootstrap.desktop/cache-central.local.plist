{
	META = {
	    contextScope = {
		host = "localhost.apple.com";
		user = "dziman"; // User scope is required as it seems some other config has priority if `host` only defined
	    };
	};

    	cacheCentralEnabled = false;
}
