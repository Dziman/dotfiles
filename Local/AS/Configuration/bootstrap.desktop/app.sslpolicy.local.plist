{
    META = {
        contextScope = {
	    host = "localhost.apple.com";
        };
    };
    // Override to file in ASWebToolkit - needed for external HTTPS
    preventRestrictingToSslOnly = true;

    // Don't force https
    sslPolicyOverride = "any";

    // Don't secure debug pages
    adminPortSecure = false;
}
