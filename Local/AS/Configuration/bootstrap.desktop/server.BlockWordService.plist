{
  META = {
    contextScope = {
      "user" = "dziman";
      "app" = "blockwordservice";
    };
  };
  "connectors+" = {
    "primary+" = {
      // Turn off TLS security for local dev
      secure = false;
    };
  };
}